import './terminal.css';

import type { JSX } from 'preact';

const Terminal = (): JSX.Element => {
	return <section className='terminal-wrapper' data-testid='terminal-wrapper'></section>;
};

export default Terminal;
